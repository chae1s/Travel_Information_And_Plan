import { createStore } from 'vuex';

/*
state: 상태 변수 - key:value로 구성
getters: state 연산, 계산 등 가져오는 함수
mutations(변이 함수 - 설정)
actions(액션 함수 - 실행)
*/

export default createStore({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    }
});